## MediaUploader-WithGem

File/Media Upload in Rails. Personalization, Authentication, Authorized access.

### SETUP

1. cd into repo
2. run `bundle install`
3. run `rake db:migrate`
4. run `rails server`


